var miModulo = require ('../index.js');

describe ("Asuite is just a function", function () {
	var = resultado;

	it ("and so is a espec", function () {
		resultado = miModulo.sumar(4,5);

		expect(resultado).toBe(9);
	});
});
